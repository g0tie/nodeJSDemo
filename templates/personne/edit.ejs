<%- include('../header.ejs') %>
<body>
    <div class="container d-flex flex-column justify-content-center align-items-center">
    <a href="/" style="position: absolute; top: 20px;left: 20px;" class="btn btn-outline-primary"> <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-house-door" viewBox="0 0 16 16">
        <path d="M8.354 1.146a.5.5 0 0 0-.708 0l-6 6A.5.5 0 0 0 1.5 7.5v7a.5.5 0 0 0 .5.5h4.5a.5.5 0 0 0 .5-.5v-4h2v4a.5.5 0 0 0 .5.5H14a.5.5 0 0 0 .5-.5v-7a.5.5 0 0 0-.146-.354L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293L8.354 1.146zM2.5 14V7.707l5.5-5.5 5.5 5.5V14H10v-4a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5v4H2.5z"/>
    </svg> Go back </a>

    <h1 class="my-5">Update infos:</h1>

    <% if (success == 1) { %>
        <p style="font-weight: bold;">DATA UPDATED !</p>
    <% } %>

    <% if (success === 0) { %>
        <p style="font-weight: bold;">ERROR!</p>
    <% } %>
  
    <form method="POST" action="/personne/<%= name %>" class="d-flex flex-column justify-content-center"  style="min-width: 400px;">
        <input class="form-control" type="text" name="username" placeholder="Nom.." value="<%= name %>" />
        <select  class="form-control" name="genre" value="<%= genre %>">
            <option disabled>Choisir un genre</option>
            <option value="F"> Femme </option>
            <option value="M"> Homme</option>
        </select>

        <select  class="form-control" name="favbrand" id="brand">
            <option disabled value="">Choisir une marque</option>
            <% for (el of brands) { %>
                <option value="<%= el.id %>"><%= el.name %></option>
            <% } %>
        </select>

        <input type="submit" value="Valider" class="btn btn-primary" />
    </form>

    <script> 
        let brandSelector = document.querySelector("#brand");
        let selectedId = "<%= brand %>";
        let indexToSelect = Array.from( brandSelector.options ).findIndex(opt => opt.value === selectedId);
        
        brandSelector.options[indexToSelect].selected = true
    </script>
</div>
</body>
</html>